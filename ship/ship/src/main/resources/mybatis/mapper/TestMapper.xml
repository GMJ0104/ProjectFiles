<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.ship.model.dao.TestMapper">

  <select id="getAllDataList" resultType="com.project.ship.model.dto.UserInfo">
    SELECT * FROM ship;
  </select>

</mapper>